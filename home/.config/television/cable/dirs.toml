[metadata]
name = "dirs"
description = "A channel to select from directories"
requirements = ["fd", "eza"]

[source]
command = ["fd -t d", "fd -t d -H -E .git -E .cache"]

[preview]
command = "eza --long --git --all --color=always --icons=always {}"

[keybindings]
shortcut = "f3"
